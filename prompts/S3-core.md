# STEP 3 — 코어 인터랙션 구현 (S-04)

> S2 구현(코어). pipeline §5 의도 선언 후 진행. F-CR-02·03·04.

## 지시 요약
- S-04 단일 화면 3상태(대기→따르기→마시기→순환) 구현.
- 따르기: 롱프레스 지속시간 → 차오름 0~100%, 가득 정지(넘침 없음).
- 마시기: 떼면 잔만, 탭마다 감소, 0이면 "캬~"+빈 잔.
- 순환·헛누름: 떼는 수위 < 8%면 마시기 안 가고 대기 복귀(8% ASSUMPTION).
- 전환은 라우트 이동 없이 요소 페이드.
- 사운드·햅틱 인터페이스 + mock(실 SDK/무음/백그라운드는 토스 WebView → UNVERIFIED).
- 문구 context §5.1 준수, 비게임(점수·목표·엔딩 없음).
- 로컬 마우스 롱프레스/탭 테스트 가능.

## 결정·가정 (ASSUMPTION)
- 헛누름 임계값 8% (context §7.3).
- 따르기 속도(점도 차등): 소주 2.0s ~ 막걸리 2.8s (F-CR-01 '점도' 최소 반영).
- 탭당 감소 25%, 비움 노출 1.2s 후 대기 복귀.

## 산출물
- src/screens/PourInteraction.jsx (전면 교체, 상태머신 + rAF + 포인터)
- src/lib/feedback.js (사운드·햅틱 mock, 설정 존중)
- src/styles/global.css (병/줄기/잔/페이드 연출 추가)

## 검증
- `npm run build` 성공(61 modules, 에러 0). dev 변환 200. 마우스 길게 누르기/탭으로 수동 확인.
