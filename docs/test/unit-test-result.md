# 폰술 단위 테스트 결과서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-06-26 |
| 실행 일시 | 2026-06-29 (Asia/Seoul) |
| 대상 커밋 | `dd3d1b3` (`dev`) + 작업 트리의 미커밋 변경 포함 |
| 실행 환경 | macOS 26.3.1, Node.js v24.14.0, npm 11.9.0 |
| 실행 명령 | `npm test` |
| 기준 계획 | `docs/test/unit-test-plan.md` |

## 1. 종합 결과

| 구분 | 파일 | 테스트 | 통과 | 실패 | 판정 |
|---|---:|---:|---:|---:|---|
| 순수 로직·저장·통신 클라이언트 | 3 | 33 | 33 | 0 | 자동화 PASS |

Vitest 전체 실행 결과 중 단위 영역은 모두 통과했다. 다만 명세와 화면 구현의 불일치가 확인되어 제품 요구사항 기준 최종 판정은 **조건부 PASS**다.

## 2. 상세 결과

| 테스트 파일 | 건수 | 결과 | 검증 내용 |
|---|---:|---|---|
| `src/lib/pour.test.js` | 13 | PASS | 수위 계산, 8% 경계, 탭 감소, 프레임 매핑 |
| `src/lib/realtimeRoom.test.js` | 15 | PASS | WebSocket URL·메시지·재연결·종료, 방 생성/종료 REST |
| `src/lib/storage.test.js` | 5 | PASS | 저장·부분 병합·설정/모드 영속 및 재진입 복원 |

## 3. 요구사항 판정

| 요구사항 | 자동화 근거 | 판정 | 비고 |
|---|---|---|---|
| F-CR-02 수위 계산·상한 | UT-CR-01 | PASS | 계산 함수 기준 |
| F-CR-03 탭 비우기 | UT-CR-03 | PASS | 4탭·0 하한 확인 |
| F-CR-04 8% 헛누름 분기 | UT-CR-02 | 부분 PASS | 순수 함수는 통과하나 화면에서 사용하지 않음 |
| F-SY-01/02 설정 지속 | UT-ST-01~02 | PASS | 현재 localStorage 어댑터 기준 |
| F-RT-01 방 생성 | UT-RT-05 | PASS | fetch mock 기준 |
| F-RT-03~05 연결·roster·cheers | UT-RT-01~03 | PASS | WebSocket mock 기준 |
| F-RT-06 종료 | UT-RT-02·04·05 | PASS | 클라이언트 분기 및 요청 형식 기준 |
| F-RT-07 재연결 | UT-RT-04 | PASS | 첫 백오프 재시도 기준 |
| 비게임 메시지 구조 | UT-RT-03 | PASS | sender/count/score/rank 미포함 |

## 4. 발견 결함 및 관찰

### D-UT-01 — 손을 뗄 때 8% 기준 상태 분기가 화면에 연결되지 않음

| 항목 | 내용 |
|---|---|
| 심각도 | Major |
| 관련 요구사항 | F-CR-02, F-CR-04 |
| 근거 | `isHollow`는 단위 테스트만 존재하고 `PourInteraction.jsx`에서 호출되지 않음 |
| 실제 동작 | 100%가 되어야 `drink` 단계로 전환하며, 8~99%에서 손을 떼면 따르기 화면에 머묾 |
| 기대 동작 | 손을 뗀 수위가 8% 미만이면 대기, 8% 이상이면 마시기 단계로 전환 |
| 상태 | OPEN · 수정 후 컴포넌트/통합 회귀 필요 |

### O-UT-01 — 테스트 실행 경고

- React Router v7 future flag 경고 2종이 stderr에 출력됐다.
- 현재 테스트 실패나 기능 오류는 아니므로 Trivial로 분류한다.

## 5. 미검증·추가 필요 항목

- `audioPolicy.js`, `feedback.js`, `share.js`, `platform.js` 전용 단위 테스트가 없다.
- 실제 앱인토스 Storage SDK가 아니라 localStorage 폴백을 검증했다. `// UNVERIFIED:`
- 실제 네트워크·서버 없이 fetch/WebSocket mock만 검증했다. `// UNVERIFIED:`
- 재연결 최대 횟수 소진, 비 JSON 메시지, 닫힌 소켓 송신 등 방어 경로의 일부 기대값이 없다.
- 임계값 8%, 탭당 25%, 재연결 간격은 제품 임의값이다. `// ASSUMPTION:`

## 6. 결론 및 재시험 조건

자동화 33건은 모두 통과했지만 D-UT-01 때문에 단위 단계의 요구사항 정합성은 완전 통과가 아니다. 다음 재시험에서는 화면 구현에 8% 분기를 연결하고, 7.99%/8%/중간 수위 해제 케이스를 컴포넌트 테스트로 추가한 뒤 전체 39건 이상을 다시 실행한다.
